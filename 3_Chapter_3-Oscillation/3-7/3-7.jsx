/*     Ok, let's combine some of the previous examples and make all of this object oriented and use vectors.    */#include vectorLib.jsx#include Oscillator.jsxvar comp = 7;var myComp = app.project.item(comp);var myFrameDuration = myComp.frameDuration;var totalFrames = myComp.duration * myComp.frameRate;var oscillators = [];var total = 10;for (i = 0;i<total;i++){     myComp.layers.addShape();        vel = createVector(r.randomRange(-.05,.05),r.randomRange(-.05,.05))    amp = createVector(r.randomRange(20,myComp.width/2),r.randomRange(20,myComp.height/2))        o = new Oscillator(comp, i+1, myComp.width , myComp.height, vel, amp);    oscillators.push(o);};for (i = 0;i<total;i++){    oscillators[i].show();};for (i = 0; i<=totalFrames;i++){            var time = myFrameDuration*i;         for (var j = 0;j < oscillators.length; j++){         oscillators[j].oscillate();             oscillators[j].display(time);                  }    };/*As always, refer to The Nature of Code byDaniel Shiffmanhttp://natureofcode.comfor original examples!*/