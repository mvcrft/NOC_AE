//Here's the Oscillator Class.#include vectorLib.jsxvar Oscillator = function(comp, layer, width, height, vel, amp){    this.comp = comp;    this.layerNum = layer;    this.angle = createVector(0,0);    this.velocity = vel;    this.amplitude = amp;    };Oscillator.prototype.oscillate = function(){    this.angle.add(this.velocity);    }Oscillator.prototype.show = function(){    layerShapeContents = app.project.item(this.comp).layer(this.layerNum).property("ADBE Root Vectors Group");    layerShapeContents.addProperty("ADBE Vector Shape - Ellipse");    layerShapeContents.property("ADBE Vector Shape - Ellipse").property("Size").setValue([100,100]);    layerShapeContents.addProperty("ADBE Vector Shape - Group");    layerShapeContents.addProperty("ADBE Vector Graphic - Stroke");    }Oscillator.prototype.display = function(time){        var x = Math.sin(this.angle.x) * this.amplitude.x;    var y = Math.sin(this.angle.y) * this.amplitude.y;        //create a shape object to push to the line    var line = new Shape();    line.vertices = [[0,0], [x,y]];        app.project.item(this.comp).layer(this.layerNum).property("ADBE Root Vectors Group").property("ADBE Vector Shape - Group").property("ADBE Vector Shape").setValueAtTime(time, line);    app.project.item(this.comp).layer(this.layerNum).property("ADBE Root Vectors Group").property("ADBE Vector Shape - Ellipse").property("Position").setValueAtTime(time,[x,y]);        }/*As always, refer to The Nature of Code byDaniel Shiffmanhttp://natureofcode.comfor original examples!*/