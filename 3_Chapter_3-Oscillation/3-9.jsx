//General init#include VectorLib.jsxvar comp = 9;var myComp = app.project.item(comp);var myFrameDuration = myComp.frameDuration;var totalFrames = myComp.duration * myComp.frameRate;//let's init our angle and periodvar startAngle = 0;var angleVel = 0.06;var amplitude = 300;// How Many Circles?var ellipseCount = 12;var increment = myComp.width/ellipseCount;//Let's setup all the ellipse shapesfor (i = 0; i <= ellipseCount; i ++ ){    myComp.layers.addShape();    var layerShapeContents = app.project.item(comp).layer(1).property("ADBE Root Vectors Group");    layerShapeContents.addProperty("ADBE Vector Shape - Ellipse");    layerShapeContents.addProperty("ADBE Vector Graphic - Stroke");        }    //first loop moves through timefor (i=0;i<=totalFrames;i++){        var time = myFrameDuration*i;    //second loop moves through layers    for(j=0; j< myComp.layers.length; j++){               startAngle += 0.03;        var angle = startAngle;                var y = map(Math.sin(angle),-1,1,0,myComp.height);                myComp.layer(j+1).position.setValueAtTime(time, [increment*j, y  ]);                angle += angleVel;        };    };/*As always, refer to The Nature of Code byDaniel Shiffmanhttp://natureofcode.comfor original examples!*/